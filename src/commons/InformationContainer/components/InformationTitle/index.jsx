// libs
import React from "react";
// others
import "./style.scss";

const InformationTitle = ({ informationTitle }) => (
  <div className="information-title-wrapper">{informationTitle}</div>
);

export default InformationTitle;

// libs
import React from "react";
// others
import "./style.scss";

const RecommendEditionTitle = ({ recommendEditionTitle }) => (
  <div className="recommend-edition-title-wrapper">{recommendEditionTitle}</div>
);

export default RecommendEditionTitle;

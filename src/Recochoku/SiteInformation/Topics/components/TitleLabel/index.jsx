// libs
import React from "react";
// others
import "./style.scss";

const TitleLabel = ({ titleLabel }) => (
  <div className="topics-title-label-wrapper">{titleLabel}</div>
);

export default TitleLabel;

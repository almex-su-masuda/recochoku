// libs
import React from "react";
// others
import "./style.scss";

const ContentTitle = ({ contentTitle }) => (
  <div className="content-title-wrapper">{contentTitle}</div>
);

export default ContentTitle;

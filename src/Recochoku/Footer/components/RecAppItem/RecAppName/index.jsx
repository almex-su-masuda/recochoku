// libs
import React from "react";
// others
import "./style.scss";

const RecAppName = ({ recAppName }) => (
  <div className="rec-app-name-wrapper">{recAppName}</div>
);

export default RecAppName;

// libs
import React from "react";
// others
import "./style.scss";

const AppCategory = ({ appCategory }) => (
  <div className="app-category-wrapper">{appCategory}</div>
);

export default AppCategory;

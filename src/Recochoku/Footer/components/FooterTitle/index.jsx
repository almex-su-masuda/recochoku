// libs
import React from "react";
// others
import "./style.scss";

const FooterTitle = ({ footerTitle }) => (
  <div className="footer-title-wrapper">{footerTitle}</div>
);

export default FooterTitle;

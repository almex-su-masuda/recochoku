// libs
import React from "react";
// others
import "./style.scss";

const FooterRinkTitle = ({ rinkTitle }) => (
  <div className="footer-rink-title-wrapper">{rinkTitle}</div>
);

export default FooterRinkTitle;

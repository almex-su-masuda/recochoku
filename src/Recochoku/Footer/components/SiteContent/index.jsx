// libs
import React from "react";
// others
import "./style.scss";

const SiteContent = ({ siteContent }) => (
  <div className="site-content-wrapper">{siteContent}</div>
);

export default SiteContent;

// libs
import React from "react";
// others
import "./style.scss";

const TopicTitleLabel = ({ titleLabel }) => (
  <div className="topic-title-label-wrapper">{titleLabel}</div>
);

export default TopicTitleLabel;

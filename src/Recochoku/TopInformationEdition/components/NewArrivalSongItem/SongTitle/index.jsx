// libs
import React from "react";
// others
import "./style.scss";

const SongTitle = ({ songTitle }) => (
  <div className="new-arrival-song-title-wrapper">{songTitle}</div>
);

export default SongTitle;
